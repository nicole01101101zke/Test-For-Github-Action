name: Simple Usage

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: lothindir/pandoc-markdown-action@v0.1
        with:
          markdown: "README.md"
          pdf: "README"
          template: "kate,monochrome"
