name: Simple Usage

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: lothindir/pandoc-markdown-action@v0.1
        with:
          markdown: "rapport.md"
          pdf: "Rapport"
          template: "kate,monochrome"
